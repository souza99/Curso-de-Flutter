import 'package:flutter/material.dart';

class PayForm extends StatelessWidget {
  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text('Cadastro de Gastos'),),
    );
  }
}